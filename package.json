{
  "name": "minimalist-portfolio-json",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "NODE_OPTIONS='--max-old-space-size=4096' astro build",
    "preview": "astro preview",
    "astro": "astro",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:integration": "vitest run --testNamePattern='integration'",
    "test:security": "vitest run --testNamePattern='security'",
    "test:solid": "vitest run --testNamePattern='solid'",
    "lint": "eslint . --ext astro,js,ts,tsx,jsx --config .eslintrc.security.cjs",
    "lint:security": "eslint . --ext .js,.ts,.astro --config .eslintrc.security.cjs || true",
    "security:audit": "npm audit --audit-level high",
    "security:scan": "npm run lint:security && npm run security:audit && node scripts/security-scanner.js",
    "security:sast": "node scripts/security-scanner.js",
    "security:dast": "npm run test:security",
    "pre-commit": "npm run test:run && npm run security:scan",
    "validate": "npm run test:run && npm run lint && npm run security:audit",
    "ci": "npm run validate",
    "cd:validate": "node scripts/cd-validator.cjs",
    "cd:validate-critical": "npm run security:scan && npm run build"
  },
  "dependencies": {
    "@astrojs/check": "0.4.1",
    "astro": "^5.15.9",
    "firebase": "^12.7.0",
    "hotkeypad": "0.1.0",
    "typescript": "5.3.3"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "@types/eslint-plugin-security": "^3.0.0",
    "eslint": "^9.39.2",
    "eslint-plugin-security": "^3.0.1",
    "eslint-plugin-xss": "^0.1.12",
    "jsdom": "^27.4.0",
    "vitest": "^4.0.16"
  }
}
