import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://statick88.github.io', // Cambia <tu-usuario> por tu nombre de usuario de GitHub
  base: '/statick88.github.io', // Asegúrate de que esta sea la ruta correcta
});


