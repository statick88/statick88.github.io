---
import { basics } from "@cv"
import Section from "@/components/Section.astro"
import Mail from "@/icons/Mail.astro"
import Phone from "@/icons/Phone.astro"
import X from "@/icons/X.astro"
import LinkedIn from "@/icons/LinkedIn.astro"
import GitHub from "@/icons/GitHub.astro"
import WorldMap from "@/icons/WorldMap.astro"

// Importamos los nuevos iconos (Asegúrate de haber creado estos archivos en src/icons/)
import Duolingo from "@/icons/Duolingo.astro"
import Chess from "@/icons/Chess.astro"
import Game from "@/icons/Game.astro"

import type { SocialIcon } from "@/types"

const { name, label, image, location, profiles, phone, email } = basics
const { city, region } = location

// Mapeamos las redes sociales a sus componentes de ícono
const SOCIAL_ICONS: SocialIcon = {
  GitHub,
  LinkedIn,
  X,
  Duolingo,
  "Chess.com": Chess,
  "Chess Universe": Game,
}

const linkedInfo = profiles.find(({ network }) => network === "LinkedIn")
const linkedUrl = linkedInfo?.url

const printInfo = [email, phone, linkedUrl].filter(Boolean).join(" • ")
---
<Section>
  <div class="container">
    <div class="info">
      <h1>{name}</h1>
      <h2>{label}</h2>
      {/* ... Tu span de localización y footers ... */}
    </div>
    <figure>
      <img src={image} alt={name} />
    </figure>
  </div>
</Section>

<style>
  .container {
    display: flex;
    flex-direction: row;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
  }

  .info {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    padding-right: 32px;
  }

  /* 🎯 REGRESA EL TAMAÑO DE FUENTE GRANDE PARA EL NOMBRE */
  h1 {
    font-size: 2rem; 
    color: var(--color-text); /* Aseguramos que el color sea del tema */
  }

  h2 {
    color: var(--color-text-secondary);
    font-weight: 500;
    font-size: 1.1rem;
    text-wrap: balance;
  }

  /* ... El resto de tus estilos (img, span, footer) ... */

  @media (width <= 700px) {
    .container {
      /* Mantiene la columna-inversa, pero añade un poco de espacio si es necesario */
      flex-direction: column-reverse;
      gap: 1.5rem; /* Pequeño ajuste en el espaciado móvil */
    }

    .info {
      justify-content: center;
      align-items: center;
      padding-right: 0;
      /* 🎯 RE-CENTRA EL TEXTO EN MÓVIL */
      text-align: center; 
    }

    figure {
      display: flex;
      justify-content: center;
      align-items: center;
      margin: 0 auto;
    }

    h1 {
        /* Asegura que el nombre sea legible en móvil */
        font-size: 1.8rem; 
    }
    
    h2 {
      text-wrap: balance;
    }
  }
</style>